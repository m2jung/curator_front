import MainView from "../../src/components/commons/units/web/main/main.container"

// 실제적인 페이지 접속 구간  (유지보수를 위해 , 셋팅 - 페이지접속 - 컨테이너 - 프레젠터)
export default function PagesMainView(){ 

//로그인을 했는지 안했는지? 이 페이지를 볼 수 있는 권한이 있는가? 로직 작성 

  return (
      <MainView/>
    )
}



